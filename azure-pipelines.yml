trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage:Build
  displayname: Build image
  jobs:
  -  job: Build
     displayName: Build
     pool:
       vmImage: 'ubuntu-latest'

